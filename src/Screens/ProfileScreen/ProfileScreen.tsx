import React from "react";
import { SafeAreaView, Text } from "react-native";

const ProfileScreen= () => {
  return(
    <SafeAreaView>
      <Text>
        Profile
      </Text>
    </SafeAreaView>
  )
};

export default ProfileScreen;
